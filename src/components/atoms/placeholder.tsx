const Placeholder = () => {
  return (
    <div className="pt-16 text-center text-sm flex items-center justify-center">
      Start by adding form items into drop container.
    </div>
  );
};

export default Placeholder;
